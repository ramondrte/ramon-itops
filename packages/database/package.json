{
  "name": "@ramon-itops/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "migrate": "npm run build && node dist/migrate.js",
    "migrate:status": "npm run build && node dist/migrate.js --status",
    "seed:demo": "npm run build && node dist/seed.js"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.6"
  }
}
