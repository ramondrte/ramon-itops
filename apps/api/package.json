{
  "name": "@ramon-itops/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "test:integration": "tsx --test test/*.integration.ts",
    "demo:prepare": "node dist/prepare-demo.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/helmet": "^13.1.1",
    "@fastify/rate-limit": "^10.3.0",
    "@ramon-itops/database": "*",
    "dotenv": "^17.2.3",
    "fastify": "^5.6.2",
    "ipaddr.js": "^2.5.0"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
